<%= render "shared/header" %>
<%= @skill.skill_name %>登録情報・編集
<%=form_with model: @skill, local: true do |f|%>
  <%= f.label :skill_name, :スキル名 %><br>
  <%= f.text_field :skill_name%><br>
  <%= f.label :skill_status, :ステータス %><br>
  <%= f.collection_select(:skill_status_id, SkillStatus.all, :id, :name, {}, {class:"select-box", id:"item-category"}) %><br>
  スキルレベル（★0～5）※スキルレベルの変更は管理者の申請が必要です。<br>
  <%= "★#{@skill.skill_level}" %><br>
  <%= f.submit "変更する" %>
<% end %>